/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com'], // Add the domain for external images
    },
  };
  
  export default nextConfig;
  